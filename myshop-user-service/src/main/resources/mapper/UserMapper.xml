<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.mhxi.myshop.user.mapper.UserMapper" >
  <resultMap id="BaseResultMap" type="top.mhxi.myshop.user.entity.User" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="password" property="password" jdbcType="VARCHAR" />
    <result column="email" property="email" jdbcType="VARCHAR" />
    <result column="phone" property="phone" jdbcType="VARCHAR" />
    <result column="role" property="role" jdbcType="VARCHAR" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long" >
    delete from user_user
    where id = #{id,jdbcType=BIGINT}
  </delete>
  <insert id="insert" parameterType="top.mhxi.myshop.user.entity.User" >
    insert into user_user (id, name, password, 
      email, phone, role, 
      create_time, update_time)
    values (#{id,jdbcType=BIGINT}, #{name,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, 
      #{email,jdbcType=VARCHAR}, #{phone,jdbcType=VARCHAR}, #{role,jdbcType=VARCHAR},
      NOW(), NOW())
  </insert>
  <update id="updateByPrimaryKey" parameterType="top.mhxi.myshop.user.entity.User" >
    update user_user
    set name = #{name,jdbcType=VARCHAR},
      password = #{password,jdbcType=VARCHAR},
      email = #{email,jdbcType=VARCHAR},
      phone = #{phone,jdbcType=VARCHAR},
      role = #{role,jdbcType=VARCHAR},

      update_time = NOW()
    where id = #{id,jdbcType=BIGINT}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long" >
    select id, name, password, email, phone, role, create_time, update_time
    from user_user
    where id = #{id,jdbcType=BIGINT}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    select id, name, password, email, phone, role, create_time, update_time
    from user_user
  </select>

  <select id="existsByUsername" resultType="java.lang.Boolean">
    SELECT COUNT(*) > 0 FROM user_user WHERE name = #{name}
  </select>

  <select id="findByUsername" resultMap="BaseResultMap">
    SELECT id, name, password, email, phone, role, create_time, update_time FROM user_user WHERE name = #{name}
  </select>

</mapper>